// search memory for a value.

#include <stdio.h>
#include <stdlib.h>

union point {
       int  *inp;
       unsigned int    *uinp;    /* pointer to a integer */
       double *
       unsigned double *udp;    /* pointer to a double */
       unsigned float  *fp;    /* pointer to a float */
       signed char   *ch     /* pointer to a char */ 
    };

int main () {
    int
    return 0;
}
